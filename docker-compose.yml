version: '3.8'

services:
  backend:
    build: ./backend
    # We removed the ports mapping here! 
    # It is now safely hidden inside the Docker network.

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend